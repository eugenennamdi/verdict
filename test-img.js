const fs = require('fs');
// Simple check: does the image contain opaque pixels at the bottom row?
// Actually, it's easier to just copy the file to a base64 string and look at it, or just use a python script to analyze it.
